z`class Solution {
public:
    void moveZeroes(vector<int>& arr) {
        int i=0,n=arr.size(),start=0;
        for(int i=0;i<n;i++){
            if(arr[i]!=0){
                swap(arr[i],arr[start++]);
            }
        }
    }
};
